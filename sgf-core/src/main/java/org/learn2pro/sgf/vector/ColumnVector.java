package org.learn2pro.sgf.vector;

/**
 * column entity in memory, a batch of column values would store in it
 * it column-based storage in memory
 */
public interface ColumnVector<T> {

    TypeLiteral
}
